[project]
name = "doba-s"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorama>=0.4.6",
    "ollama>=0.3.3",
    "pydantic>=2.9.2",
    "python-dotenv>=1.0.1",
]
